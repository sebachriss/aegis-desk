# Imagen del UI legacy de Aegis Desk (Streamlit).
# Construir: docker build -f Dockerfile.ui -t aegis-desk-ui .
FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

RUN groupadd -r aegis && \
    useradd -r -g aegis -s /bin/false -d /app aegis && \
    mkdir -p /app/data && \
    chown -R aegis:aegis /app

RUN python -m venv /app/.venv
ENV PATH="/app/.venv/bin:$PATH"

COPY --chown=aegis:aegis requirements.txt requirements-lock.txt ./
RUN pip install -r requirements-lock.txt

COPY --chown=aegis:aegis . .

USER aegis

EXPOSE 8501

HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8501/_stcore/health')" || exit 1

CMD ["streamlit", "run", "ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
